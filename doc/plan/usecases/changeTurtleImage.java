// button is pressed by user to either upload a turtal image to use or choose from pre-esisting images

// if images are preset, they would be stored in a css file

// if the decision is made to upload images, image files can be sotred in a data folder to be accessed

// screen displays new turtle image